server:
  port: 8025
  servlet:
    context-path: /security

spring:
  datasource:
      url:  jdbc:mysql://localhost:3306/practice?useUnicode=true&characterEncoding=UTF-8&useSSL=true&serverTimezone=GMT%2B8
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  type-aliases-package: com.plf.learn.security

logging:
  level:
    com:
      plf:
        learn:
          security:
            mapper: debug
